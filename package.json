{
  "name": "site-spono",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace frontend",
    "start": "concurrently \"npm run start --workspace backend\" \"npm run start --workspace frontend\"",
    "lint": "node --check scripts/dev.mjs && npm run lint --workspace frontend && npm run lint --workspace backend",
    "test": "npm run test --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
